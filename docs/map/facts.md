# 用户确定事实（User-Confirmed Facts）

> 记录用户在本项目开发过程中拍板确定的事实。**active 事实 = 已确认的开发约束——禁止破坏**；
> 开发与事实冲突 → 停下升级用户决策；用户方向变动 → 同步评估受影响事实、询问用户后更新。
> 状态：`active` ｜ `superseded`；superseded 必须保留冲突处理记录（用户决策历史，勿删）。
> 每条建议带「约束范围」（反引号路径/模块/关键词）供 check 检测变更是否触及。

## 事实

<!-- 模板：
### [F-001] 事实标题
- 状态：active
- 确认日期：YYYY-MM-DD
- 约束范围：`<路径或模块>`、`<关键词>`
- 事实：<用户确定的内容，禁止破坏>
- 冲突处理：（active 留空；superseded 填：原由 / 新方向 / 用户决策）
-->

### [F-001] 蒸馏子代理唤醒判定 = 会话任务停止时间（可配置，默认 10 分钟），非任务结束即唤醒
- 状态：active
- 确认日期：2026-09-05
- 约束范围：`插件 lib/src`、`唤醒触发`、`蒸馏子代理`、`idle 判定`
- 事实：唤醒蒸馏子代理的判定是**会话任务停止（静默/空闲）达到可配置时长**后才触发，**不是执行任务结束马上唤醒走蒸馏固化**；该时长是自定义配置项，**默认 10 分钟**（600s），后续会添加设置 UI。
- 冲突处理：（无）

### [F-002] 蒸馏子代理模型可配置（独立于会话模型）
- 状态：active
- 确认日期：2026-09-05
- 约束范围：`插件 config`、`llmProvider/llmModel`、`蒸馏子代理模型`
- 事实：蒸馏子代理使用的模型**必须可单独配置**（默认跟随会话当前模型，但支持指定本实例已配置的其他模型）；后续会添加设置 UI 配置项。
- 冲突处理：（无）

### [UI 开发待办] 两个配置项的 UI
- 状态：active
- 确认日期：2026-09-05
- 约束范围：`设置 UI`、`客户端插件配置面板`
- 事实：F-001 的「唤醒空闲时长」与 F-002 的「蒸馏子代理模型」两个自定义配置项，后续需添加**设置 UI**（用户明确「这两个记住 ui 开发的待办」）。
- 冲突处理：（无）
